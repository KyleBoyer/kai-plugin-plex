{
  "name": "kai-plugin-plex",
  "version": "0.1.10",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --dev",
    "watch": "node esbuild.config.mjs --dev --watch",
    "test": "npm run build && node tests/run-tests.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.0"
  }
}
