{
  "name": "plex",
  "displayName": "Plex & Media Stack",
  "version": "0.1.1",
  "description": "Dashboard and controls for Plex, Sonarr, Radarr, Tautulli, downloads, and more",
  "author": "Kyle Boyer",
  "icon": {
    "lucide": "Tv"
  },
  "permissions": [
    "config:read",
    "config:write",
    "state:publish",
    "ui:settings",
    "ui:panel",
    "ui:navigation",
    "network:fetch",
    "notifications:send",
    "tools:register",
    "safe-storage"
  ],
  "configSchema": {
    "type": "object",
    "properties": {
      "plex": {
        "$ref": "#/$defs/service"
      },
      "radarr": {
        "$ref": "#/$defs/service"
      },
      "sonarr": {
        "$ref": "#/$defs/service"
      },
      "tautulli": {
        "$ref": "#/$defs/service"
      },
      "bazarr": {
        "$ref": "#/$defs/service"
      },
      "prowlarr": {
        "$ref": "#/$defs/service"
      },
      "seer": {
        "$ref": "#/$defs/service"
      },
      "sabnzbd": {
        "$ref": "#/$defs/service"
      },
      "qbittorrent": {
        "$ref": "#/$defs/service"
      },
      "tdarr": {
        "$ref": "#/$defs/service"
      },
      "wizarr": {
        "$ref": "#/$defs/service"
      }
    },
    "$defs": {
      "service": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string"
          },
          "apiKey": {
            "type": "string"
          },
          "enabled": {
            "type": "boolean",
            "default": true
          }
        }
      }
    }
  }
}
